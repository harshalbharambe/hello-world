create table table1 (id int IDENTITY(1,1) NOT NULL, name varchar(50), CONSTRAINT PK_table1 PRIMARY KEY (id));
create table table2 (id int IDENTITY(1,1) NOT NULL, name varchar(50), CONSTRAINT PK_table2 PRIMARY KEY (id));
create table table3 (id int IDENTITY(1,1) NOT NULL, name varchar(50), CONSTRAINT PK_table3 PRIMARY KEY (id));